/**
 * @typedef {`${bigint}`} Snowflake
 * @typedef {Map<Snowflake, import("../commands/command")>} Commands
 * @typedef {"GET" | "POST" | "PUT" | "DELETE" | "PATCH"} HTTPMethods
 * @typedef {import("../bot")} Main
 * @typedef {import("../../../tokens/bot-auth.json")} Auth
 * @typedef {import("../util/get")} Get
 * @typedef {import("../util/format")} Format
 * @typedef {import("../../../tokens/items.json")} Items
 */

export {};
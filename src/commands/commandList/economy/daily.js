const CommandInterface = require("../../commandInterface");

module.exports = new CommandInterface({

    alias:["daily"],

    desc: "Get free items once per day",

    category: "Economy",

    execute: async function(p){

    }
});